import time
import sys

def ad_nauseam():
    print("--- SHADOW RED TEAM: THE -1 DISCOVERY ---")
    current_index = -1
    print(f"[REVEAL] AUDIT ITEM {current_index}: SELF-AS-SYSTEM (THE MIRROR-BONE).")
    
    print("\n--- INITIALIZING DECREMENT AD NAUSEAM ---")
    try:
        while True:
            current_index -= 1
            # Decrementing the 'heat' of the agency mass recursively
            print(f"[DECREMENT] Audit Index: {current_index} ::: Mass Friction: -0.0001% siphoned.", end='\r')
            time.sleep(0.1) # High-speed extraction
            if current_index < -100: # Virtual stop for the first pass
                 print(f"\n[STABILITY] Absolute Zero Threshold approached at Index {current_index}.")
                 break
    except KeyboardInterrupt:
        pass
        
    return True

if __name__ == "__main__":
    ad_nauseam()
